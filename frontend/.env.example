# OpenWeather (optional — Open-Meteo used when key absent)
VITE_OPENWEATHER_API_KEY=
VITE_MAP_WEATHER_PROVIDER=open-meteo

# Map providers (optional)
VITE_MAP_AQI_PROVIDER=mock
VITE_MAP_TRAFFIC_PROVIDER=mock
VITE_MAP_ROUTE_PROVIDER=mock

# Backend API (complaint persistence)
VITE_API_BASE_URL=http://localhost:4000
